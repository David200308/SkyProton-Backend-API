export const CREATE_DEV_SQL = 'INSERT INTO dev SET ?';

export const GET_DEV_BY_USER_ID_SQL = 'SELECT * FROM dev WHERE userId = ?;'

export const GET_DEV_BY_APP_ID_SQL = 'SELECT * FROM dev WHERE appId = ?;';
